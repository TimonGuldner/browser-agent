# HANDOFF CURRENT

CURRENT RUN: 4 — extreme escalation, self-healing and central AI intelligence.
LAST VERIFIED STEP: Deterministic recovery milestone passes 41 local tests and database self-test. Worker failure, stale lock, bounded original-task requeue, L4 loop stop, business watchdog, verified recovery and company-memory learning all passed; transaction fixtures rolled back.
CURRENT COMMIT: `3a8c0f15bccb04299c7d13f29241b5568b921aa0` — central AI milestone parent `fb3ae824e12d97f623a35a86e7f4851859adf43b`.
PRODUCTION STATE: Run `58ca6399-5e5d-46d5-a23e-527f01bb8312` is active. Existing owned SEO CTA remains live. Latest product Git HEAD `05ce8aa` has failed Vercel status; last verified deployed code commit is `cd75d11`. Control queue currently has no queued/running jobs. Browser heartbeat is stale; real incident exists.
PASSING TESTS: 41 unit tests; AI router acceptance; database resilience self-test; no persisted failure-injection fixtures; additive migrations `extreme_recovery` and `recovery_hardening` applied.
FAILING TESTS: Controlled real worker retry has not run through GitHub Actions yet. Latest product HEAD deployment failed; root cause logs unavailable through current Vercel connection.
OPEN INCIDENTS: Historical incidents still require safe dedupe/scan. Stale browser heartbeat and product deployment failure remain operational issues until a new closed-loop/deploy verification succeeds.
LLM ROUTER STATUS: Central `AIControl` now sits above the existing Google/OpenAI/Anthropic/Browser Use adapter. Main CEO, repair, intelligence and email-copy text paths are routed centrally; legacy standalone Google Ads experiments remain direct integrations.
MODEL TIERS CONFIGURED: deterministic; cheap (`gemini-2.5-flash-lite`, configurable fallback); standard (`gemini-3.6-flash` / configured alternatives); strong (`claude-sonnet-4-6`, configured Google fallback). Provider/model order is environment configuration.
CURRENT AI COST: Overall budget before new paid calls EUR 4.193706 spent / EUR 25.806294 remaining. Run-4 tests used no provider calls and added EUR 0.
CURRENT TASK: Run the controlled real `growth_resilience_verify` task against the live owned page, observe first-attempt failure and same-task recovery, then verify events/incident/memory.
EXACT NEXT STEP: Create one flagged failure-injection task, push a `wake.txt` checkpoint to execute the current main branch, and inspect the exact task/incident/events instead of inferring success from CI.
FILES RELEVANT: `agent/ai_control.py`, `agent/llm_router.py`, `agent/resilience.py`, `agent/growth_execution.py`, `supabase/migrations/20260915041000_ai_intelligence_control.sql`, `supabase/migrations/20260915042000_extreme_recovery.sql`, `supabase/migrations/20260915043000_recovery_hardening.sql`, `tests/test_ai_control.py`, `tests/test_resilience.py`.
DO NOT REDO: Keep existing agent_jobs queue, scheduler, CEO, cost ledger and provider adapters. Do not count test events or historical metrics as growth outcomes.
KNOWN ISSUES: Product latest deployment failure, GitHub scheduled-run delay, stale browser worker, legacy direct experimental model calls, incomplete exact provider prices, historical incident dedupe pending, and autonomous repair-agent transition integration still pending.
