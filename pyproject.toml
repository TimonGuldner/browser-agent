[project]
name = "timon-browser-agent"
version = "0.1.0"
description = "Self-hosted Browser Use worker with Supabase job queue"
requires-python = ">=3.12,<4.0"
dependencies = [
  "browser-use==0.13.10",
  "supabase>=2.0,<3.0",
  "python-dotenv>=1.0,<2.0",
  "pydantic-settings>=2.0,<3.0",
]

[tool.ruff]
line-length = 120
