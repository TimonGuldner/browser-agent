[project]
name = "timon-browser-agent"
version = "0.2.0"
description = "Cloud-only Browser Use runner with Supabase control plane for LOCENIX"
requires-python = ">=3.12,<4.0"
dependencies = [
  "supabase>=2.0,<3.0",
  "httpx>=0.27,<1.0",
  "python-dotenv>=1.0,<2.0",
  "pydantic-settings>=2.0,<3.0",
]

[tool.ruff]
line-length = 120
